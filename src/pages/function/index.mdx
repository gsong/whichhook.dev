import { Link } from "gatsby";

# Create Callback Function

Does the function really need to be in the component?

- <Link to="function/callback-or-not">Yes</Link>, it depends on incoming props or values from hooks
- No. Then hoist it outside of the component.
