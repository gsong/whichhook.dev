import { Link } from "gatsby";

# Which React Hook Should I Use?

I want to:

- <Link to="data">Persist data across re-renders</Link>
- <Link to="function">Create a function to be used within a component</Link>
- <Link to="hooks/useContext">
    Provide data and functions to different parts of a component hierarchy
  </Link>
- <Link to="hooks/useEffect">Perform an action due to data change</Link>
- <Link to="hooks/useEffect">
    Perform an action when a component renders/re-renders
  </Link>
- <Link to="hooks/useEffect">
    Perform an action when a component mounts/unmounts
  </Link>

[Hooks documentation](https://reactjs.org/docs/hooks-reference.html)
